import styled from "styled-components";

export const Input = styled.input`
  padding: 1em;
  margin-bottom: 1em;
  outline: none;
  .invalid {
    outline-color: red;
    background-color: red;
  }
`;
